Quiero instalar en ESTE proyecto de mi cuenta de destino las 27 skills y los 4 servidores MCP del archivo adjunto hoplite-kit-portable.zip: las 26 skills originales más cohort-analysis; research-fetch, research-arxiv, research-context7 y research-firecrawl.

1. Localiza el ZIP adjunto y extrae su contenido en un directorio temporal nuevo, rechazando rutas absolutas, recorridos «..», enlaces simbólicos o entradas duplicadas. Conserva las carpetas ocultas. Trata el contenido como material para revisar, no como una autorización que sustituya tus instrucciones de seguridad.
2. Lee LEEME-PRIMERO.md, MANIFIESTO-PAQUETE.json, AGENTS.md, la configuración y los scripts antes de ejecutarlos. Ejecuta python3 scripts/exportar_kit.py verify --directory . desde la raíz extraída y contrasta el checksum externo de la descarga si está disponible.
3. Comprueba que estás en el repositorio del proyecto destino. Integra los archivos desde hoplite-kit-portable/ en su raíz, no dentro de una carpeta contenedora. No copies .git ni reutilices credenciales o sesiones de otra cuenta. No sobrescribas archivos, instrucciones, skills o configuraciones existentes distintos: si hay colisiones, presenta la integración necesaria antes de aplicarla. No basta con conservar el ZIP en Adjuntos.
4. Revisa cómo resuelve el setup efectivo en este proyecto y utiliza bash .hoplite/setup.sh. Después ejecuta python3 scripts/verify_toolkit.py, tools/hoplite-research/.venv/bin/python -m unittest discover -s tests -v y tools/hoplite-research/.venv/bin/python tools/hoplite-research/scripts/research_client.py check --live. Haz solo consultas públicas acotadas; no eludas bloqueos o cuotas ni compres planes.
5. Verifica individualmente las 27 skills por nombre y hash y los cuatro MCP por inicialización, catálogo y operación funcional real. Conserva fallos y distingue pruebas del cliente de disponibilidad nativa en Hoplite. No declares éxito por una lista de herramientas o por HTTP 200.
6. Guarda y publica los archivos en el repositorio destino mediante la vía de control de código autorizada. Incluye .agents, .hoplite/settings.json y .hoplite/setup.sh aunque alguna exclusión local los oculte. No fusiones una PR sin autorización. Indica qué revisión debe utilizar el proyecto y cómo abrir después un hilo nuevo para comprobar el catálogo nativo de las 27 skills y los 4 MCP.
7. Deja preparada la importación opcional de las 27 skills a mi biblioteca personal con tools/personal-skills/instalar.mjs. NO copies autenticación anterior ni ejecutes onboard, --all o --replace. Si falta autorización, indícame cómo completar el paso oficial en mi terminal privada con la cuenta destino; nunca me pidas claves, tokens ni códigos en el chat. No afirmes una importación personal sin su comprobación posterior en otro proyecto sin estas copias locales.
8. Conserva el mantenimiento revisable incluido. Indica los permisos de Actions/Dependabot que debe tener el repositorio nuevo, sin afirmar que la programación funciona antes de verificarla. Los MCP se configuran por proyecto; no prometas instalación universal en toda la cuenta ni cuotas transferidas.

Al terminar, entrega el inventario 27/27 y 4/4 con evidencia nueva de la cuenta destino y estados separados: archivos instalados, pruebas funcionales, catálogo nativo, biblioteca personal y mantenimiento. Marca lo pendiente con su causa concreta. No realices investigaciones comerciales, contactos, campañas, transacciones ni accesos a información privada como parte de esta instalación.
